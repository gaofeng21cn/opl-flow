interface:
  display_name: "OPL Flow"
  short_description: "Codex workflow for OPL App"
  default_prompt: "Use $opl-flow to install, sync, or diagnose the workflow profile and verify it through fresh readback."
